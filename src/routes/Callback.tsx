// Placeholder — step 2 wires this up to call
// userManager.signinRedirectCallback() and complete the code exchange.
function Callback() {
  return <p>Completing sign-in…</p>;
}

export default Callback;